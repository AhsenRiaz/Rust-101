fn main() {
    let coord = (2, 3);
    println!("{}, {}", coord.0, coord.1);

    let (x, y) = (10, 20);
    println!("{}, {}", x, y);

    let user_info = ("Bob", 21);
    println!("{}, {}", user_info.0, user_info.1);
}
